# Backend test toolchain — pinned, reproducible set (spec: local-backend-testing, T1.4)
#
# Verified working in a clean backend/.venv (python3.11) with PYTHONNOUSERSITE=1.
# All three import checks pass with this set:
#   python -c "import OpenSSL.crypto"          (no GEN_EMAIL AttributeError)
#   python -c "from cryptography import x509"  (no _rust import error)
#   python -c "import moto, boto3, hypothesis, pytest"
#
# Notes on the pins:
#  - pyOpenSSL is pinned EXPLICITLY. Modern moto no longer depends on it
#    transitively, so omitting it reintroduces the GEN_EMAIL crash (R1.3).
#    pyOpenSSL 24.2.1 + cryptography 43.0.3 is the coherent, compatible pair.
#  - boto3/botocore 1.34.0 match the runtime pins in backend/requirements.txt
#    (boto3==1.34.0, botocore==1.34.0) — no divergence from the deployed runtime.
#  - requests 2.31.0 also matches the runtime pin in backend/requirements.txt.

pytest==9.1.1
pytest-mock==3.15.1
boto3==1.34.0
botocore==1.34.0
moto==5.0.14
hypothesis==6.168.0
cryptography==43.0.3
pyOpenSSL==24.2.1
Pillow==12.3.0
PyJWT==2.9.0
bcrypt==4.2.1
requests==2.31.0
